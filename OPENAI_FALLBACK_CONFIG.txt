# TEMPORARY OpenAI CONFIGURATION
# Replace the AMD LLM configuration (lines 18-23) with this:

llm = ChatOpenAI(
    model="gpt-4",  # or "gpt-3.5-turbo" for faster/cheaper
    api_key=os.getenv("OPENAI_API_KEY"),
    temperature=0.2,
    max_tokens=2048,
)

# When you get AMD access, switch back to:
# llm = ChatOpenAI(
#     model="meta/llama-3-70b-instruct",
#     base_url=os.getenv("AMD_API_BASE_URL"),
#     api_key=os.getenv("AMD_API_KEY"),
#     temperature=0.2,
#     max_tokens=2048,
# )
